

    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">EC 1805</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <div class="collapse navbar-collapse justify-content-end" id="navbarsExampleDefault">


                <ul class="navbar-right nav navbar-nav" style="color: white; margin-left: 1em;">
                    <li class="dropdown nav-item">
                      <% if(locals.user) { %>
     
                        <% } %>
                        <% if(!locals.user) { %>
                      <a href="/signin" class="dropdown-toggle nav-link" role="button" style="color: white;" aria-expanded="false"><i class="fa fa-fw fa-bell-o"></i>
                        Đăng nhập </a>
                        <% } %>
                    </li>
            
                    <% if(locals.user) { %>
                    <li class="dropdown" ><a class="dropdown-toggle nav-link" data-toggle="dropdown" href="#" >Xin chào
                        <strong>
                          <%= user.username %></strong><span class="caret"></span></a>
                      <ul role="menu" class="dropdown-menu" style="padding: 1em;">
                          <hr style="margin: 0">
                        <li class="active"><a class="animate" href="/profile">Hồ sơ</a></li>
                        <hr style="margin: 0">
                        <li class="active"><a class="animate" href="/history-purchase">Lịch sử mua hàng</a></li>
                        <% if(locals.user.agency) { %>
                          <hr style="margin: 0">
                          <li class="animate"><a class="animate" href="/dashboard">Quản lý nhà xe</a></li>
                          <% } %>
                        <% if(locals.user.admin) { %>
                            <hr style="margin: 0">
                            <li class="animate"><a class="animate" href="/admin">Trang quản trị viên</a></li>
                        <% } %>
                          <hr style="margin: 0">
                        <hr style="margin: 0">
                        <li><a class="animate" href="#">Đổi mật khẩu</a></li>
                        <hr style="margin: 0">
                        <li><a class="animate" href="/logout">Đăng xuất</a></li>
                      </ul>
                    </li>
                    <% } %>
                  </ul>

            </div>
        </div>
    </nav>